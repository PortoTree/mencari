{
  "name": "mencari.online-monorepo",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter api run start:dev",
    "dev:web": "pnpm --filter web run dev",
    "db:generate": "prisma generate",
    "db:push": "prisma db push"
  },
  "devDependencies": {
    "prisma": "^5.21.0"
  },
  "dependencies": {
    "@prisma/client": "^5.21.0"
  }
}
